# new-api 前端 UI 美化方案：Neon Glass 控制台

> 文档版本：V2，高还原演示站风格  
> 主要参考：[T.RICKS Glassmorphism](https://tricks-glassmorphism.webflow.io/)  
> 辅助参考：[Reflect](https://reflect.app/)  
> 适用范围：`web/` 公共首页、登录页、控制台、数据管理页与系统设置  
> 设计关键词：深黑、霓虹、玻璃、透光、悬浮、超大字、实验感

## 1. 改版目标

本方案不是对现有后台做轻量润色，而是要让 new-api 的视觉明显接近 T.RICKS Glassmorphism 演示站：

- 全局使用近黑背景和多层霓虹光场。
- 玻璃拟态成为主要视觉材质，而不是局部装饰。
- 使用高透明面板、背景模糊、内高光和发光描边建立空间深度。
- 使用荧光粉、酸性绿、亮黄和电光青形成强烈对比。
- 在首页、登录页和概览页使用超大标题、宽字距和实验性排版。
- 在按钮、筛选、卡片、图表和状态变化中加入明显但连贯的动效。
- 保留 new-api 的表格、筛选、日志和设置能力，但让它们处于统一的 Neon Glass 世界中。

最终视觉应让用户在第一眼就感知到：这是一个具有未来感、实验感和实时流动感的 AI API 控制台，而不是传统白底 SaaS 后台。

## 2. 参考站风格拆解

### 2.1 T.RICKS Glassmorphism 的核心特征

演示站最重要的视觉语言不是单个玻璃卡片，而是一套完整组合：

1. **近黑舞台背景**：背景接近纯黑，内容像灯光装置一样浮在空间中。
2. **大面积霓虹光场**：粉色、酸性绿、黄绿色在背景中交叠，给玻璃层提供透光基础。
3. **超大实验字体**：标题具有极大的字号、宽字距和舞台感，形成强烈品牌记忆。
4. **悬浮玻璃面板**：卡片使用半透明背景、模糊、白色细描边、内高光和深色投影。
5. **前后景穿插**：标题、光场、玻璃卡片和装饰文字并非平铺，而是有明确的空间前后关系。
6. **异色强调**：粉色与酸性绿同时出现，局部加入亮黄，不采用单一紫色主题。
7. **控件可视化**：滑杆、计数器、开关和价格结果本身就是视觉主体。
8. **悬浮与视差**：背景移动慢、面板移动快，鼠标和滚动会改变层次。

### 2.2 Reflect 的辅助价值

Reflect 不作为主视觉参考，只提取以下内容：

- 将真实产品界面作为首屏核心，而不是使用抽象插画。
- 用深色空间和聚光区域集中用户注意力。
- 用一段连续演示展示产品如何工作。
- 页面区块之间保持电影镜头式连续过渡。

### 2.3 new-api 的转化方式

T.RICKS 的信用卡和报价计算器，在 new-api 中分别转化为：

- 信用卡式玻璃对象 → API Key、账户余额、渠道身份或模型访问卡。
- 报价计算器 → Token/费用计算器、渠道权重调节器、充值金额选择器。
- 大标题舞台 → 公共首页、登录页、控制台欢迎区和空状态。
- 霓虹光场 → 模型流量、请求路径、渠道状态和计费热度的视觉背景。
- 荧光控件 → 主操作、选中状态、成功状态、实时请求和在线渠道。

## 3. 总体视觉方向

### 3.1 视觉命名

本方案统一命名为 **Neon Glass Console**。

它由四个视觉层组成：

- **Stage**：近黑背景和低亮纹理。
- **Glow Field**：粉、绿、黄、青的柔化光带或光域。
- **Glass Surface**：所有主要界面面板的半透明材质。
- **Signal Layer**：状态、数据、按钮和图表的高亮信号。

### 3.2 色彩占比

建议桌面端视觉占比：

| 色彩类型 | 占比 | 用途 |
| --- | ---: | --- |
| 黑与炭灰 | 62% | 舞台背景、深层面板、代码区 |
| 透明白与冷灰 | 20% | 玻璃面板、描边、正文 |
| 霓虹粉 | 7% | 品牌高光、主按钮、关键路径 |
| 酸性绿 | 5% | 在线、成功、实时流量 |
| 亮黄 | 3% | 警告、余额、注意事项 |
| 电光青 | 3% | 信息、模型、延迟与图表 |

页面不能只剩紫色和深蓝。霓虹粉、酸性绿、亮黄与电光青必须共同构成视觉节奏。

### 3.3 明暗模式策略

本设计以深色模式为品牌默认模式，并提供浅色玻璃模式：

- 深色模式是完整 Neon Glass 体验。
- 浅色模式使用冰白玻璃、深色文字和较低饱和的彩色光场。
- 系统模式继续跟随操作系统。
- 现有主题预设保留，但新 Neon Glass 预设作为推荐预设，不删除其他预设。

## 4. 页面视觉骨架

### 4.1 桌面控制台

```text
┌─────────────────────────────────────────────────────────────────────┐
│ LOGO      DASHBOARD  MODELS  STATUS        ◌ Search  ◉  ◐  Avatar │
├───────────────┬─────────────────────────────────────────────────────┤
│               │  O V E R V I E W                                  │
│  ◈ Overview   │  Run every model through one luminous gateway      │
│  ◇ Models     │                                                     │
│  ◇ API Keys   │  ╭──────────────────────────────────────────────╮   │
│               │  │  Balance     Requests      Uptime     Errors │   │
│  PERSONAL     │  │  $128.42     2.4M          99.98%     0.02%  │   │
│  ◇ Logs       │  ╰──────────────────────────────────────────────╯   │
│  ◇ Wallet     │                                                     │
│               │  ╭────────────────────╮ ╭────────────────────────╮ │
│  ADMIN        │  │  REQUEST FLOW      │ │  LIVE CHANNELS         │ │
│  ◇ Channels   │  │  glowing chart     │ │  status + latency      │ │
│  ◇ Users      │  ╰────────────────────╯ ╰────────────────────────╯ │
│  ◇ Settings   │                                                     │
└───────────────┴─────────────────────────────────────────────────────┘
```

背景光场位于应用壳层后方，侧栏、顶部栏和内容面板均可透出背景颜色。内容不能使用完全不透明的大白块切断空间感。

### 4.2 移动控制台

```text
┌──────────────────────────┐
│ ☰  new-api       ◌  ◉   │
├──────────────────────────┤
│ O V E R V I E W          │
│ AI gateway, live now     │
│                          │
│ ╭──────────────────────╮ │
│ │ Balance     $128.42  │ │
│ │ Requests       2.4M  │ │
│ │ Uptime        99.98% │ │
│ ╰──────────────────────╯ │
│                          │
│ ╭──────────────────────╮ │
│ │ Request flow         │ │
│ │ neon area chart      │ │
│ ╰──────────────────────╯ │
│                          │
│ ╭──────────────────────╮ │
│ │ Channel item         │ │
│ ╰──────────────────────╯ │
└──────────────────────────┘
```

移动端减少模糊半径和视差幅度，但保留色彩、描边和玻璃材质。

## 5. 背景系统

### 5.1 全局 Stage

深色背景不是单一纯黑，应由以下层级构成：

1. `#050506` 基础底色。
2. 中心区域使用轻微灰黑径向提亮。
3. 使用低透明网格、扫描线或噪点增加材质。
4. 页面顶部和关键模块后方放置霓虹光域。
5. 最上层添加极弱暗角，使焦点留在内容区。

### 5.2 光场形态

不使用独立小圆球。光效采用宽幅、裁切、拉伸的结构形态：

- 横向霓虹带。
- 从屏幕边缘进入的椭圆光域。
- 沿请求路径移动的光线。
- 在玻璃面板后方缓慢流动的模糊色块。
- 与图表峰值对应的局部辉光。

推荐位置：

- 首页 Hero：粉色位于左上，酸性绿位于右下，中间有少量亮黄过渡。
- 控制台：顶部粉青光带，侧栏底部绿色光域。
- 钱包：亮黄和粉色光域。
- 渠道监控：绿色与青色光域，异常区域出现红粉色。
- 日志：青色请求流，错误状态出现红粉短脉冲。

### 5.3 背景实现示例

```css
.neon-stage {
  position: relative;
  isolation: isolate;
  background:
    linear-gradient(rgba(255, 255, 255, 0.018) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255, 255, 255, 0.018) 1px, transparent 1px),
    radial-gradient(ellipse 58% 42% at 16% 8%, rgba(255, 50, 173, 0.26), transparent 68%),
    radial-gradient(ellipse 52% 38% at 88% 24%, rgba(184, 255, 44, 0.20), transparent 72%),
    radial-gradient(ellipse 46% 34% at 58% 92%, rgba(24, 218, 255, 0.15), transparent 74%),
    #050506;
  background-size: 48px 48px, 48px 48px, auto, auto, auto, auto;
}

.neon-stage::after {
  position: fixed;
  inset: 0;
  z-index: -1;
  pointer-events: none;
  content: '';
  background: radial-gradient(circle at center, transparent 48%, rgba(0, 0, 0, 0.56) 100%);
}
```

所有颜色最终应通过主题 token 引用。这里的数值用于定义目标视觉，不要求直接复制到生产代码。

## 6. Neon Glass 材质系统

### 6.1 四级玻璃材质

| 级别 | 名称 | 用途 | 透明度 | 模糊 |
| --- | --- | --- | ---: | ---: |
| G0 | Clear Glass | 大面积内容、表格 | 90-94% | 8px |
| G1 | Soft Glass | 普通卡片、侧栏 | 78-86% | 14px |
| G2 | Glow Glass | 指标、表单、工具栏 | 68-78% | 20px |
| G3 | Hero Glass | Hero、登录、余额、弹窗 | 52-68% | 28px |

“透明度”表示面板自身不透明程度。越重要、越靠前的面板允许透出更多背景光，但正文区域必须保留对比。

### 6.2 标准玻璃面板

```css
.glass-panel {
  position: relative;
  overflow: hidden;
  background:
    linear-gradient(135deg, rgba(255, 255, 255, 0.13), rgba(255, 255, 255, 0.035)),
    rgba(12, 12, 15, 0.64);
  border: 1px solid rgba(255, 255, 255, 0.22);
  border-radius: var(--radius);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.26),
    inset 0 -1px 0 rgba(255, 255, 255, 0.04),
    0 24px 80px rgba(0, 0, 0, 0.42),
    0 0 0 1px rgba(255, 255, 255, 0.025);
  backdrop-filter: blur(20px) saturate(145%);
}

.glass-panel::before {
  position: absolute;
  inset: 0;
  pointer-events: none;
  content: '';
  background: linear-gradient(118deg, rgba(255, 255, 255, 0.18), transparent 24%, transparent 72%, rgba(255, 255, 255, 0.06));
}
```

### 6.3 发光边框

主要交互卡片允许使用局部发光边框：

```css
.neon-border {
  border: 1px solid transparent;
  background:
    linear-gradient(rgba(10, 10, 13, 0.72), rgba(10, 10, 13, 0.72)) padding-box,
    linear-gradient(120deg, rgba(255, 60, 180, 0.78), rgba(255, 255, 255, 0.20) 46%, rgba(189, 255, 54, 0.72)) border-box;
}
```

使用范围：

- 当前选中的模型或渠道。
- 余额卡和主指标卡。
- 登录表单。
- 激活的筛选面板。
- 鼠标悬浮的主要功能卡。

普通表格行不使用完整发光边框，只使用左侧信号线或微弱背景光。

### 6.4 高光扫过

可点击大卡片在 Hover 时允许出现一次高光扫过：

```css
.glass-panel-interactive::after {
  position: absolute;
  top: -40%;
  left: -55%;
  width: 34%;
  height: 180%;
  pointer-events: none;
  content: '';
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.18), transparent);
  transform: rotate(16deg);
  transition: left 520ms cubic-bezier(0.16, 1, 0.3, 1);
}

.glass-panel-interactive:hover::after {
  left: 125%;
}
```

高光只能由真实交互触发，静态面板不重复播放。

## 7. 色彩 Token

建议在 `web/src/styles/theme.css` 中增加以下语义 token，并让 `theme-presets.css` 支持覆盖：

```css
:root {
  --neon-stage: oklch(0.105 0.008 285);
  --neon-surface: oklch(0.16 0.012 285 / 0.74);
  --neon-surface-strong: oklch(0.19 0.014 285 / 0.90);
  --neon-border: oklch(1 0 0 / 0.20);
  --neon-highlight: oklch(1 0 0 / 0.28);

  --neon-pink: oklch(0.70 0.28 350);
  --neon-pink-soft: oklch(0.70 0.28 350 / 0.24);
  --neon-lime: oklch(0.90 0.23 125);
  --neon-lime-soft: oklch(0.90 0.23 125 / 0.20);
  --neon-yellow: oklch(0.92 0.20 98);
  --neon-cyan: oklch(0.80 0.18 210);
  --neon-red: oklch(0.65 0.25 25);

  --neon-text: oklch(0.97 0.01 285);
  --neon-text-muted: oklch(0.74 0.02 285);
  --neon-shadow: 0 28px 90px rgb(0 0 0 / 0.46);
}
```

### 7.1 色彩语义

| Token | 业务语义 |
| --- | --- |
| `--neon-pink` | 品牌、主操作、当前导航、费用主线 |
| `--neon-lime` | 正常、在线、成功、实时输出 |
| `--neon-yellow` | 余额、额度、警告、待处理 |
| `--neon-cyan` | 信息、模型、延迟、请求流量 |
| `--neon-red` | 错误、失败、危险操作 |

主色与状态色可以同时出现，但不能交换语义。例如失败状态不能因为主题是粉色而继续使用品牌粉表达。

## 8. 字体与排版

### 8.1 三层字体系统

1. **Display**：公共首页、登录页、概览欢迎区的大标题。
2. **Interface**：导航、表格、表单和正文。
3. **Mono**：API Key、请求、日志、金额、Token、延迟和代码。

现有 Public Sans 可继续作为 Interface。Display 应优先从现有字体中选择较有几何感的粗体，避免仅为展示效果新增过大字体包。

### 8.2 演示站式标题

公共首页可使用拆字和宽字距：

```text
N E W  A P I
CONTROL EVERY MODEL
THROUGH ONE GLASS GATEWAY
```

控制台标题则收敛为：

```text
O V E R V I E W
CHANNEL MONITOR
USAGE SIGNALS
```

规则：

- 首页 H1：桌面 `clamp(64px, 9vw, 144px)`，移动端 `48-64px`。
- 控制台欢迎标题：桌面 `32-48px`，不占用超过首屏 22% 高度。
- 页面常规标题：`20-24px`。
- 卡片标题：`13-15px`，可使用大写和 `0.08em-0.14em` 字距。
- 正文和按钮保持正常字距，避免所有文字都被拉开。
- 金额和指标使用 `tabular-nums`。

### 8.3 装饰文字

允许在背景中加入超大轮廓文字，例如：

- `ROUTE`
- `RELAY`
- `TOKENS`
- `UPTIME`
- `NEURAL TRAFFIC`

装饰文字需要：

- `aria-hidden="true"`。
- 不影响正文对比。
- 不作为用户必须阅读的信息。
- 在移动端隐藏或大幅缩小。

## 9. 圆角、描边与阴影

参考演示站的卡片轮廓，本方案不采用过度圆润的 SaaS 胶囊风：

| 元素 | 圆角 |
| --- | ---: |
| 小按钮、输入框、Badge | 6px |
| 工具栏、普通卡片 | 8px |
| 主要玻璃面板 | 10px |
| Hero 产品面板、登录面板 | 12px |
| 圆形图标按钮、头像 | 999px |

描边规则：

- 默认玻璃边框：`rgba(255,255,255,.16-.22)`。
- Hover：提高到 `.30-.36`。
- Active：加入对应霓虹色边缘辉光。
- Disabled：降低透明度，不改变尺寸。
- Error：红色描边 + 文本 + 图标，不只使用红光。

阴影应向下扩散且较深，模拟悬浮玻璃，不使用浅灰色 SaaS 卡片阴影。

## 10. 应用壳层

现有结构：`AppHeader + AppSidebar + SidebarInset + AnimatedOutlet`。

目标文件：

- `web/src/components/layout/components/authenticated-layout.tsx`
- `web/src/components/layout/components/app-header.tsx`
- `web/src/components/layout/components/app-sidebar.tsx`
- `web/src/components/layout/components/section-page-layout.tsx`

### 10.1 顶部栏

- 使用 G2 玻璃材质。
- 高度由现有紧凑高度增加至建议 `56px`，形成更明确舞台边缘。
- 品牌标识左侧增加小型粉绿渐变信号灯或运行脉冲。
- 顶部导航使用宽字距大写文本，当前项显示霓虹粉底线。
- 搜索框使用透明玻璃输入，聚焦时出现粉青双色描边。
- 通知、主题、语言、配置和用户按钮使用透明圆形图标按钮。
- Header 下方可有一条极细的动态光线，缓慢从粉色过渡到绿色。

### 10.2 侧边栏

- 使用 G1 或 G2 玻璃材质，背景透出左侧光场。
- 导航组标题使用小号大写宽字距。
- 导航图标默认冷白，Hover 变为青色，Active 变为粉色或酸性绿。
- 当前项使用透明高亮底、左侧 2px 发光线和轻微内高光。
- 折叠态仍保留发光当前项，并使用 Tooltip 显示名称。
- 管理员分组底部可出现淡红粉光域，与普通导航形成区域差异。
- 设置下钻导航保持原有 URL 逻辑，但加入顶部返回箭头和大写分组名。

### 10.3 内容画布

- `SidebarInset` 本身保持透明，让全局 Stage 可见。
- 页面内容应在背景光场上方分层，不增加全屏不透明内容底。
- 页面标题区不包卡片，可与背景超大装饰文字叠加。
- 主要内容使用玻璃面板组合。
- 路由切换使用淡入、轻微上移和背景光场位移。

## 11. 公共首页

目标文件：

- `web/src/features/home/index.tsx`
- `web/src/features/home/components/sections/hero.tsx`
- `web/src/components/layout/components/public-header.tsx`

### 11.1 首屏

首页必须首先呈现高冲击 Neon Glass 舞台，而不是传统左右两栏营销页。

推荐结构：

```text
                    N E W  A P I
              ONE GATEWAY. EVERY MODEL.

        [ Start routing ]      [ Explore models ]

             ╭──────────────────────────╮
             │ POST /v1/chat/completions│
             │ model: gpt-5             │
   PINK      │ route → healthy channel  │      LIME
   LIGHT     │ 182ms  ·  $0.0034        │      LIGHT
             ╰──────────────────────────╯

           40+ PROVIDERS  ·  LIVE ROUTING  ·  BILLING
```

视觉要求：

- 产品名或明确品类必须是 H1。
- 标题直接位于舞台中，不放进卡片。
- 主视觉是一个大型 G3 玻璃控制台/终端界面。
- 背景中出现大号轮廓文字和粉绿光场。
- 主面板随鼠标有轻微 3D 倾斜，最大不超过 `3deg`。
- 面板内部循环演示请求进入、渠道选择、流式响应和费用记录。
- 首屏底部露出下一节提供商或实时数据。

### 11.2 浮动公共导航

- 初始状态透明，滚动后变为 G3 胶片式玻璃导航。
- 导航边框带顶部内高光。
- 主 CTA 使用霓虹粉底色，Hover 向酸性绿过渡或出现双色光边。
- 移动端打开全屏玻璃菜单，菜单项使用大字号纵向排布。

### 11.3 提供商区

- 不使用传统卡片网格。
- 品牌图标放在一条或多条横向流动轨道上。
- 轨道穿过透明玻璃层，前后透明度不同。
- Hover 某提供商时显示对应模型数量、状态和平均延迟。
- 支持 `prefers-reduced-motion` 时停止自动移动。

### 11.4 工作流区

使用三块交错玻璃面板展示：

1. 请求进入。
2. 智能路由。
3. 结算与日志。

滚动到该区域时，粉色请求光点沿连接线移动，经过路由节点后变为绿色，最终进入账单面板。

### 11.5 CTA 区

- 使用大号轮廓字 `START ROUTING` 作为背景。
- 前景只放标题、一句说明和一个主按钮。
- 玻璃终端从底部露出，形成页面连续性。

## 12. 登录与注册页

登录页是 Neon Glass 风格的重要入口，应接近演示站的玻璃信用卡视觉。

推荐布局：

- 全屏近黑舞台。
- 左上粉色光域，右下酸性绿光域。
- 中央悬浮 G3 登录面板，比例接近信用卡或控制终端。
- 面板顶部显示 new-api 标识和在线信号。
- 输入框像玻璃刻槽，聚焦时边缘发光。
- 第三方登录按钮使用透明玻璃底，品牌图标保留原色。
- Passkey 登录使用明确的指纹/密钥图标和青色信号。
- 面板后方可放大号轮廓字 `ACCESS`。

登录错误时不震动整个卡片，只让错误字段和顶部状态线短暂红色脉冲。

## 13. 控制台概览

目标文件：`web/src/features/dashboard/components/overview/overview-dashboard.tsx`

### 13.1 顶部欢迎舞台

概览页顶部采用小型 Hero：

- 背景装饰字：`OVERVIEW` 或 `LIVE GATEWAY`。
- 标题：当前时间段或欢迎信息。
- 副标题：当前服务状态摘要。
- 右侧：时间范围、刷新、创建 API Key 或充值。
- 背景光场根据健康状态变化：正常偏绿青，存在异常时混入红粉。

### 13.2 核心指标玻璃带

将余额、请求量、Token、成功率和延迟放入一条横向 G2 玻璃带，而不是五张互不相关的普通卡片。

每个指标包含：

- 大号等宽数字。
- 小号宽字距标题。
- 迷你趋势线。
- 环比变化。
- 对应霓虹色信号。

指标之间使用透明分隔线。Hover 某个指标时，其背景光增强，其他指标不缩放。

### 13.3 请求流量图

- 使用玻璃图表容器。
- 面积图从青色过渡到粉色。
- 峰值点使用酸性绿光点。
- 网格线使用低透明白线。
- Tooltip 使用 G3 玻璃面板。
- 当前时间指示线可有轻微脉冲。
- 加载时使用流动扫描线骨架，不使用普通灰块闪烁。

### 13.4 渠道状态盘

- 每个渠道显示品牌图标、名称、在线状态、延迟和成功率。
- 在线状态用绿点和柔和外发光。
- 降级状态用黄点。
- 故障状态用红点和短周期脉冲。
- 频道列表本身使用一块玻璃面板，条目通过分隔线划分，不为每条再创建卡片。

### 13.5 接入指引

接入指引改为玻璃终端：

- Tabs 切换 Curl、Node.js、Python 和 Go。
- 当前 Tab 使用粉色下划线。
- 代码区接近黑色但保留轻微背景透光。
- 复制成功后按钮短暂变为酸性绿。
- 终端顶部使用三枚小状态点，但不模仿特定操作系统窗口。

## 14. 渠道监控

目标文件：

- `web/src/features/channels/index.tsx`
- `web/src/features/channels/components/channels-table.tsx`

### 14.1 顶部监控带

页面顶部展示：

- 正常渠道。
- 异常渠道。
- 测试中。
- 平均延迟。
- 今日失败请求。

整条监控带使用 G2 玻璃，并在后方显示一条横向绿青光流。异常数量大于 0 时，对应区域出现红粉光。

### 14.2 工具栏

- 搜索框使用宽玻璃输入。
- 筛选、分组、显示敏感信息、视图和列设置使用玻璃图标按钮。
- 激活筛选时按钮出现霓虹边框和数量 Badge。
- 添加渠道使用实心霓虹粉按钮。
- 批量测试使用酸性绿描边按钮。

### 14.3 表格

表格整体使用 G0 Clear Glass：

- 表头半透明吸顶。
- 行之间使用极细白色透明分隔线。
- Hover 行背景出现横向柔光，不抬升行。
- 当前选择行使用左侧粉色发光线。
- 正常状态为酸性绿，测试中为电光青，禁用为冷灰，异常为红色。
- 延迟字段使用迷你发光条，而不是只有数字。
- 测试运行时，整行出现从左向右移动的扫描线。

### 14.4 卡片视图

渠道卡片使用 G2：

- 左上角品牌图标。
- 右上角状态信号。
- 中间显示渠道名称和关键配置。
- 底部显示延迟、权重和余额。
- Hover 时产生 `translateY(-4px)` 和局部高光扫过。
- 卡片位置不使用随机旋转，保持数据页面整齐。

## 15. API Keys

目标文件：`web/src/features/keys/index.tsx`

API Key 页面应借鉴演示站的玻璃信用卡：

- 桌面卡片视图中，每个 Key 可呈现为横向玻璃身份卡。
- Key 前缀类似卡号排版，使用等宽大字和宽间距。
- 左上显示名称，右上显示状态信号。
- 底部显示分组、余额限制、过期时间和最近使用时间。
- 卡片后方根据状态透出不同光场。
- 默认遮罩密钥，显示和复制分别使用眼睛、复制图标按钮。
- Hover 时轻微倾斜和高光扫过。

表格视图继续保留，用于大量 Key 的高效管理。

创建成功 Dialog：

- 使用 G3 玻璃。
- 密钥值置于强对比代码槽。
- “仅展示一次”使用亮黄状态条。
- 复制成功后状态条切换为酸性绿。
- 不允许霓虹效果弱化安全提示。

## 16. 使用日志

目标文件：`web/src/features/usage-logs/index.tsx`

### 16.1 视觉概念

日志页定位为“请求信号监视器”：

- 页面背景出现低透明横向扫描线。
- 实时日志到达时，顶部信号点短暂闪烁。
- 筛选栏使用 G2 玻璃。
- 日志表使用 G0 玻璃，保持高密度。

### 16.2 日志行

- 成功请求：左侧青绿信号线。
- 慢请求：左侧黄色信号线。
- 失败请求：左侧红粉信号线。
- 流式请求：模型名称旁出现小型流动图标。
- Token、费用和耗时使用等宽数字。
- Hover 时行背景由左到右出现很浅的光带。

### 16.3 日志详情

详情使用大型 G3 Sheet 或 Dialog：

- 顶部显示请求状态、模型、渠道、用户和总费用。
- 请求、响应、计费和管理信息使用玻璃 Tabs。
- JSON 代码区使用不透明深黑背景，边框透出青色光。
- 复制、折叠、换行和下载使用图标按钮。
- 错误位置使用红色行高亮，不给整个代码区加红色背景。

## 17. 钱包与充值

目标文件：`web/src/features/wallet/index.tsx`

### 17.1 余额卡

余额卡是最接近演示站信用卡的模块：

- 使用 G3 Hero Glass。
- 比例建议 `1.7:1`。
- 后方是粉黄光场。
- 左上显示 `AVAILABLE BALANCE`。
- 中央大号金额使用等宽字体。
- 右上显示账户状态和实时信号。
- 底部显示预计可用天数和本月消耗。
- Hover 时轻微 3D 倾斜，高光随指针移动。

### 17.2 充值计算器

直接借鉴演示站报价计算器的结构：

- 金额使用发光 Slider。
- 常用金额使用减号、加号和数值步进器。
- 支付方式使用玻璃 Toggle 或 Radio Group。
- 优惠、手续费和到账金额实时更新。
- 最终到账金额使用超大亮黄或酸性绿数字。
- “立即充值”使用霓虹粉实心按钮。

### 17.3 其他钱包模块

- 消耗趋势使用粉黄面积图。
- 订阅计划使用并排玻璃方案卡。
- 支付方式使用品牌图标玻璃条目。
- 兑换码输入使用玻璃长输入框。
- 推广奖励使用酸性绿信号，不与主余额竞争。

## 18. 系统设置

目标文件：

- `web/src/features/system-settings/components/settings-page.tsx`
- `web/src/components/layout/config/system-settings.config.ts`

设置页不能回到传统白底表单，应保持 Glass Console 风格：

- 设置导航使用 G1 玻璃侧栏。
- 当前设置组在内容区顶部用大写宽字距显示。
- 每个主要设置分组使用一块 G1/G2 玻璃面板。
- 面板内部使用透明分隔线，不为每个字段再嵌套卡片。
- Switch 打开时轨道为酸性绿，关闭时为透明冷灰。
- Slider 使用粉色轨道和白色玻璃滑块。
- Segmented Control 选中项使用透明白底和霓虹边缘。
- Select/Popover 使用 G3 玻璃。
- 保存栏吸附在底部，使用半透明强玻璃。
- 有未保存修改时，保存栏出现黄色信号；保存成功切换为绿色。
- 危险区使用暗红玻璃和红色描边，但保持文字清晰。

## 19. 数据表与工具栏统一规范

目标基础文件：`web/src/components/data-table/layout/data-table-page.tsx`

### 19.1 Page Toolbar

```text
╭──────────────────────────────────────────────────────────────────╮
│ ◌ Search...   [Filter 3] [Group] [Columns]      [▦][≡]  [+ Add] │
╰──────────────────────────────────────────────────────────────────╯
```

- 整体使用 G2 玻璃。
- 输入和按钮内部仍透明，依靠内描边区分。
- 激活控件使用对应霓虹色。
- 表格/卡片视图使用图标分段控制。
- 批量操作出现时不改变工具栏高度。

### 19.2 表格容器

- 使用一整块 G0 玻璃，不在外部再套 Card。
- 表头使用 `position: sticky` 和更强不透明度。
- 分页位于底部玻璃栏。
- 大数据量仍优先使用虚拟滚动。
- 玻璃材质不能引起行文字对比不足。

### 19.3 空状态

空状态也应接近演示站风格：

- 背景显示大号轮廓文字，例如 `NO SIGNAL`。
- 前景显示单个玻璃状态面板。
- 图标有微弱霓虹轮廓。
- 只保留一个主行动和一个重置筛选操作。
- 不使用通用插画或过度可爱的图形。

## 20. 基础组件规范

### 20.1 Button

| Variant | 视觉 |
| --- | --- |
| Primary | 霓虹粉实心底、深色文字或高对比白字、外发光 |
| Success | 酸性绿实心/描边，用于测试与确认成功 |
| Secondary | 透明玻璃底、白色描边 |
| Ghost | 透明，无边框，Hover 出现玻璃高光 |
| Destructive | 红色描边或实心，仅用于破坏性操作 |
| Icon | 固定正方形，圆形或 6px 圆角，带 Tooltip |

交互：

- Hover：亮度提高，向上移动 `1-2px`。
- Press：回到原位并略微降低亮度。
- Loading：按钮宽度不变，出现发光 Spinner。
- Focus：显示明显双色焦点环。
- Disabled：去除辉光并降低饱和度。

### 20.2 Input

- 深色透明玻璃槽。
- 默认白色低透明描边。
- Hover 增强描边。
- Focus 使用粉青双色描边和内高光。
- Error 使用红色描边、图标和错误文字。
- 自动填充状态需覆盖浏览器默认黄色背景。

### 20.3 Card

- 普通卡片默认为 G1。
- 重要指标使用 G2。
- Hero、余额和登录使用 G3。
- 可点击 Card 才具有抬升、倾斜和高光扫过。
- Card 内不再嵌套完整 Card，使用透明分区或分隔线。

### 20.4 Badge 与状态灯

- Badge 背景透明，边框和文字使用状态色。
- 在线状态可以有柔和呼吸效果。
- 错误状态使用短脉冲，不持续快速闪烁。
- 状态必须同时显示文字，不能只显示颜色灯。

### 20.5 Dialog、Sheet、Popover

- 默认使用 G3。
- Overlay 使用深黑透明遮罩，同时保留少量背景光。
- Dialog 打开时背景光场轻微缩小和变暗。
- Popover 从触发器方向位移 `4-8px` 进入。
- 移动端大型 Dialog 转为全高玻璃 Sheet。

### 20.6 Tabs 与 Segmented Control

- 容器为透明玻璃凹槽。
- Active 项使用半透明白底、霓虹下划线或发光滑块。
- 滑块平移动画为 `180-240ms`。
- 不用多个圆角文字按钮模拟 Tabs。

### 20.7 Tooltip 与 Toast

- Tooltip 使用 G3、小圆角、清晰白字。
- Toast 使用强玻璃表面和左侧状态信号线。
- 成功 Toast 为绿色，错误 Toast 为红色，警告为黄色。
- 多个 Toast 不遮挡顶部导航和主要操作。

## 21. 图表系统

### 21.1 颜色映射

| 数据 | 颜色 |
| --- | --- |
| 请求量 | 电光青 |
| Token | 霓虹粉 |
| 费用 | 亮黄 |
| 成功率 | 酸性绿 |
| 错误率 | 霓虹红 |
| 延迟 | 青色到黄色渐变 |

### 21.2 图表视觉

- 面积图允许透明霓虹渐变。
- 折线有轻微外发光，但线条本身保持清晰。
- 网格线透明度低于 `.08`。
- 坐标文字为次级冷白。
- Tooltip 使用 G3。
- 图例使用小型状态灯和文字。
- 图表切换时使用路径和透明度过渡，不整体缩放容器。
- 图表空状态显示 `NO SIGNAL`，并提供时间范围或刷新操作。

### 21.3 图表性能

- 不使用无限粒子动画。
- 折线辉光使用有限阴影，不重复叠加多层滤镜。
- 图表离开视口后暂停动画。
- 大数据量降采样或聚合后展示。

## 22. 动效与视差

### 22.1 动效等级

| 等级 | 场景 | 幅度 |
| --- | --- | --- |
| M1 | 按钮、输入、导航 | 轻微亮度、位移 1-2px |
| M2 | 卡片、Popover、Tabs | 位移 4-8px、透明度、局部高光 |
| M3 | Dialog、Sheet、页面切换 | 位移 12-24px、缩放 0.98-1 |
| M4 | 首页 Hero、余额卡 | 轻微 3D 倾斜、视差、光场移动 |

### 22.2 时间规范

- Hover/Press：`100-160ms`。
- Tooltip/Popover：`160-220ms`。
- Dialog/Sheet：`220-320ms`。
- 路由切换：`260-360ms`。
- Hero 光场循环：`8-16s`。
- 在线呼吸灯：`1.8-2.8s`。
- 高光扫过：单次 `420-600ms`。

### 22.3 页面切换

建议 `AnimatedOutlet` 使用：

1. 旧页面透明度下降并向后缩小到 `0.99`。
2. 背景光场横向移动 `12-20px`。
3. 新页面从下方 `12px` 淡入。
4. 总时长控制在 `320ms` 内。

### 22.4 鼠标视差

只用于：

- 首页主视觉。
- 登录玻璃卡。
- 钱包余额卡。
- API Key 卡片视图。

限制：

- 最大旋转 `3deg`。
- 最大位移 `8px`。
- 离开后平滑归位。
- 触摸设备关闭。
- `prefers-reduced-motion` 下关闭。

## 23. 响应式策略

### 23.1 桌面端

- 完整显示背景装饰字、光场、侧栏玻璃和多列面板。
- 表格使用高密度模式。
- 允许 Hero 卡片视差和复杂图表光效。

### 23.2 平板端

- 侧栏切换为图标态或抽屉。
- 背景光场数量减少。
- 指标带改为两列。
- 工具栏允许两行，但按钮热区和高度保持稳定。

### 23.3 移动端

- 关闭复杂视差和大面积 `backdrop-filter` 动画。
- 保留粉、绿、黄的静态光场。
- 玻璃模糊半径降低到 `10-14px`。
- 数据表切换为玻璃列表卡。
- 大标题按固定断点缩小，不随视口连续缩放。
- Dialog 变为全屏或底部 Sheet。
- 装饰文字不得与正文重叠。

### 23.4 超长文本

- 导航、按钮、Badge 和卡片标题需要验证最长语言。
- 不使用负字距压缩文字。
- 宽字距 Display 标题在移动端允许换行。
- API Key、请求 ID 和模型名使用中间省略或横向滚动。

## 24. 可访问性

强视觉风格不能牺牲可访问性：

- 正文达到 WCAG AA 对比度。
- 玻璃面板的实际背景变化下都要验证文字可读性。
- 所有图标按钮提供可访问名称和 Tooltip。
- 焦点环不能被霓虹边框吞没。
- 状态必须同时使用颜色、图标和文字。
- 呼吸、扫描、视差和循环光效支持 `prefers-reduced-motion`。
- 闪烁频率不得触发光敏风险。
- Dialog 正确锁定焦点，关闭后返回触发器。
- 装饰文字、光域和扫描线使用 `aria-hidden` 或纯 CSS。
- 图表提供文本摘要或数据表替代。

## 25. 性能与降级

### 25.1 性能边界

- 同一视口内持续动画的光场不超过 3 个。
- 长表格行不使用独立 `backdrop-filter`，整张表共享玻璃容器。
- 卡片高光使用伪元素和 transform，不实时计算复杂滤镜。
- 鼠标视差使用 `requestAnimationFrame` 和 CSS 变量，并进行节流。
- 图表光效不使用高成本多重模糊。
- 移动端和低性能设备减少模糊、阴影和背景动画。

### 25.2 降级策略

```css
@supports not (backdrop-filter: blur(1px)) {
  .glass-panel {
    background: rgba(15, 15, 18, 0.94);
  }
}

@media (prefers-reduced-motion: reduce) {
  .neon-motion,
  .glass-panel-interactive::after {
    animation: none !important;
    transition-duration: 1ms !important;
  }
}
```

### 25.3 验证指标

- 主要控制台页面滚动保持流畅。
- 表格筛选和切页不因光效出现明显延迟。
- 页面切换无明显布局位移。
- 首页 LCP 资源经过压缩和按需加载。
- Lighthouse Accessibility 目标不低于 95。

## 26. 与现有工程结合

### 26.1 主题系统

目标文件：

- `web/src/styles/theme.css`
- `web/src/styles/theme-presets.css`
- `web/src/styles/index.css`
- `web/src/context/theme-provider.tsx`
- `web/src/context/theme-customization-provider.tsx`
- `web/src/lib/theme-customization.ts`

实施方式：

- 新增 `neon-glass` 主题预设。
- 深色下提供完整效果，浅色下提供冰玻璃效果。
- 保留用户对字体、圆角、密度、内容宽度和侧栏布局的配置。
- 所有业务组件通过语义 token 获取颜色，不硬编码固定粉绿值。

### 26.2 基础组件

目标目录：`web/src/components/ui/`

建议扩展现有 CVA variant：

- `surface`: `clear-glass | soft-glass | glow-glass | hero-glass`。
- `glow`: `none | pink | lime | yellow | cyan | danger`。
- `motion`: `none | lift | shine | pulse`。

不要为每个视觉效果创建新的重复组件。

### 26.3 页面框架

目标文件：

- `web/src/components/layout/components/section-page-layout.tsx`
- `web/src/components/data-table/layout/data-table-page.tsx`

统一提供：

- 页面装饰标题插槽。
- 页面级光场配置。
- Glass Toolbar。
- Glass Content Surface。
- 页面进入动效。
- reduced-motion 降级。

## 27. 实施优先级

### P0：建立 Neon Glass 世界

1. 新增 Neon Glass 主题 token 和预设。
2. 建立全局 Stage、光场和四级玻璃材质。
3. 改造 Button、Input、Card、Dialog、Popover、Tabs 和 Tooltip。
4. 改造顶部栏、侧边栏和页面内容画布。
5. 完成响应式与 reduced-motion 降级。

### P1：形成第一批高辨识页面

1. 公共首页 Hero。
2. 登录/注册页。
3. 控制台概览页。
4. 钱包余额卡与充值计算器。
5. API Key 玻璃身份卡。

### P2：统一工作台页面

1. 渠道监控。
2. 使用日志。
3. DataTablePage。
4. 系统设置。
5. 其他用户和管理页面。

### P3：精细动效与性能

1. Hero 请求演示。
2. 路由光流动画。
3. 视差和高光扫过。
4. 图表霓虹过渡。
5. 性能分析和低性能降级。

## 28. 分阶段实施计划

### 阶段 1：主题与材质

- 增加 Neon Glass token。
- 增加深色和浅色方案。
- 实现 Stage、Glow Field、G0-G3 玻璃类。
- 为现有基础组件增加视觉 variant。

验收：组件预览中可以明显看出与演示站同类的玻璃、描边和霓虹效果。

### 阶段 2：壳层

- 改造 Header、Sidebar、SidebarInset 和 SectionPageLayout。
- 添加全局光场和页面切换动画。
- 适配全部侧栏布局、密度和 RTL。

验收：进入任一控制台页面均能感知统一 Neon Glass 世界，不出现传统白底区域。

### 阶段 3：首页与登录

- 重写 Hero 舞台。
- 添加真实 API 请求演示。
- 添加提供商流动轨道和请求工作流。
- 改造登录注册玻璃卡。

验收：首屏视觉与 T.RICKS 演示站在色彩、材质、空间和动效上接近，同时明确展示 new-api 产品能力。

### 阶段 4：概览、钱包与 API Keys

- 实现指标玻璃带。
- 实现霓虹图表和渠道状态盘。
- 实现余额卡和充值计算器。
- 实现 API Key 玻璃卡片视图。

验收：三类核心页面具有强辨识度，不再只是替换背景颜色。

### 阶段 5：渠道、日志、表格和设置

- 统一 Glass Toolbar 和 Clear Glass Table。
- 添加行信号、扫描线和状态光。
- 改造设置表单与保存栏。
- 保持高密度和虚拟滚动性能。

验收：高频后台操作完整可用，视觉风格与首页一致。

### 阶段 6：测试与优化

- 视觉回归测试。
- 动效降级测试。
- 多主题、多语言、多视口测试。
- 性能分析和合成层检查。
- 清理重复硬编码样式。

## 29. 验收矩阵

### 29.1 视觉验收

- [ ] 页面使用近黑舞台而非普通深灰后台背景。
- [ ] 首屏同时存在霓虹粉、酸性绿和至少一种辅助色。
- [ ] 主要面板具有透明度、模糊、描边、内高光和投影。
- [ ] 首页或欢迎区具有演示站式超大实验标题。
- [ ] 背景、玻璃面板和前景信号存在明确前后层次。
- [ ] Hover、Active、Focus 和 Loading 有完整霓虹反馈。
- [ ] 钱包、API Key 或登录页至少有一种信用卡式玻璃对象。
- [ ] 数值调节或充值区域具有演示站计算器式控件。
- [ ] 数据页没有退回到完全不透明的传统 SaaS 表格风格。
- [ ] 移动端仍保留玻璃与霓虹身份，而不是完全移除视觉设计。

### 29.2 功能验收

- [ ] 表格、筛选、批量操作和分页步骤不增加。
- [ ] 超长文字不溢出按钮、导航和卡片。
- [ ] 玻璃背景下正文对比度合格。
- [ ] 键盘焦点清晰可见。
- [ ] 动效不会改变组件稳定尺寸。
- [ ] reduced-motion 下所有核心流程可用。
- [ ] 低性能降级后仍有正确色彩和层级。
- [ ] 明暗模式和主题切换无闪白。
- [ ] i18n 长文本不会与装饰标题重叠。
- [ ] 所有状态不只依赖颜色。

### 29.3 视口验收

至少检查：

| 视口 | 用途 |
| --- | --- |
| 375x812 | 移动端核心流程 |
| 768x1024 | 平板与折叠侧栏 |
| 1280x720 | 低高度桌面 |
| 1440x900 | 标准桌面 |
| 1920x1080 | 超宽舞台与内容宽度 |

### 29.4 工程验收

前端实现后至少执行：

```bash
cd web
bun run typecheck
bun run lint
bun run build
```

布局、焦点、键盘、加载、空状态、错误状态、移动视图和 reduced-motion 变更应按照 `web/AGENTS.md` 补充对应行为测试。

## 30. 禁止偏离项

以下情况视为没有达到本方案目标：

- 只把现有卡片改成 `bg-white/10`，没有背景光场和空间层次。
- 只增加紫色渐变，没有粉、绿、黄、青的异色对比。
- 玻璃只出现在首页，控制台仍是普通实体后台。
- 只有静态视觉，没有 Hover、Focus、路由和状态动效。
- 首页仍使用传统左右分栏 Hero。
- 使用大量普通圆角卡片网格代替演示站式悬浮构图。
- 为追求性能完全取消玻璃，而不是按 G0-G3 和设备能力降级。
- 为追求视觉效果牺牲表格可读性、键盘操作或移动端任务。
- 在业务页面到处复制硬编码粉绿样式，不接入主题 token。

## 31. 最终效果定义

完成后的 new-api 应具备以下直观感受：

- 打开首页，看到接近 T.RICKS 的深黑霓虹舞台、超大排版和悬浮玻璃主视觉。
- 进入登录页，看到像玻璃身份卡一样的访问面板。
- 进入控制台，背景光场、玻璃导航和玻璃数据面板保持同一世界观。
- 查看概览时，数据像实时信号一样发光和流动。
- 管理渠道和日志时，仍然能快速扫描大量信息，但状态更鲜明、界面更有层次。
- 充值和创建 API Key 时，能够直接联想到演示站的玻璃信用卡和计算器交互。

一句话定义：**把 T.RICKS Glassmorphism 的霓虹舞台、玻璃材质和实验交互完整移植到 new-api，再用 Reflect 的真实产品演示方式呈现 API 网关能力。**
