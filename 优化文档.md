# 前端 UI 优化文档

## 1. 文档目的

本文汇总当前 Neon Glass UI 的实机检查结果、代码审查结论与配色优化建议，作为后续修复和迭代的执行清单。

检查范围：

- 公开首页：桌面端与移动端，亮色、暗色模式。
- 注册页：短视口移动端。
- 主题默认值与主题切换流程。
- 公共玻璃材质、数据表与 API Key 卡片视图。
- 不支持 `backdrop-filter` 时的兼容性降级。

本文不替代 `newapi的前端UI美化文档.md`。原文档定义视觉方向；本文记录现有实现的问题、修复优先级和验收标准。

## 2. 当前结论

Neon Glass 的视觉方向可以保留：暗色舞台、半透明表面、紫色主操作，以及粉、绿、黄、青作为信号色，已经形成明确的产品识别度。

当前优先问题不在于整体风格，而在于以下四点：

1. 两处真实可用性回归阻断了用户流程。
2. 亮色模式的正文和表面层级不足。
3. 暗色模式紫色使用过密，功能语义不够清晰。
4. 玻璃材质和动画的全局作用范围过大，容易影响非 Neon Glass 主题和兼容环境。

## 3. 优先修复项

### P0：首页主标题永久不可见

**现象**

首页语义结构中存在产品名称 H1，但亮色和暗色模式下都不可见。实测其计算样式为 `opacity: 0`，导致品牌标题缺失，并在 `JPGAPI` 装饰字和副标题之间形成明显的纵向空洞。

**根因**

`Hero` 的 H1 同时使用了：

- `opacity-0` 与 `landing-animate-fade-up`，用于入场动画。
- `neon-hero-title`，用于持续的渐变 shimmer 动画。

`neon-hero-title` 使用 `animation` 简写设置了 shimmer，覆盖了 `landing-animate-fade-up` 的 `animation`，因此 H1 不会再从透明状态过渡到可见状态。

**涉及文件**

- `web/src/features/home/components/sections/hero.tsx`
- `web/src/styles/index.css`
- `web/src/styles/neon.css`

**建议修复方式**

优先选择以下任一种方式，避免同一元素被两个 `animation` 简写竞争：

1. 删除 H1 的 `opacity-0`，让 shimmer 独立生效。
2. 将 shimmer 放到 H1 内部的 `span` 或伪元素，外层 H1 只负责入场动画。
3. 将两个动画显式组合为 animation list，并确保入场动画保留 `forwards` 或 `both` 填充模式。

**验收标准**

- 亮色、暗色、减少动态效果三种场景下，H1 均在首屏可见。
- 产品名称和副标题之间不存在无意义的大块留白。
- 刷新页面后 H1 不会停留在透明状态。

### P0：短视口认证页面不可滚动

**现象**

在 `390 x 600` 视口下访问注册页，提交按钮位于可视区域之外，页面不允许垂直滚动。实测 `body.scrollHeight` 等于 `body.clientHeight`，内容被直接裁掉。

**根因**

认证布局根容器使用了 `h-svh overflow-hidden`。当注册表单的实际高度高于视口时，容器禁止了唯一可用的滚动路径。

**涉及文件**

- `web/src/features/auth/auth-layout.tsx`

**建议修复方式**

- 根容器改用 `min-h-svh`，而不是固定 `h-svh`。
- 移除根容器的 `overflow-hidden`，或明确改为 `overflow-y-auto`。
- 给认证内容容器增加移动端上下内边距，确保输入框、验证码、协议和提交按钮始终可达。
- 小屏或短视口禁用 `TiltCard` 的透视变换，只保留静态卡片表面，避免裁剪和绘制异常。

**验收标准**

- `390 x 600`、手机横屏、浏览器 200% 缩放下，注册、登录、找回密码等页面都能滚动到最后一个可交互控件。
- Tab 键焦点移动到最后一个控件时，浏览器会将该控件带入可见区域。
- 页面不出现表单内容重复切片、裁剪或无法点击的状态。

## 4. 高优先级质量改进

### 4.1 无毛玻璃能力时的亮色降级错误

**现象**

在不支持或禁用 `backdrop-filter` 的环境中，玻璃表面、弹窗、Popover、Sheet 与 Tooltip 会被统一改为固定深色背景。亮色主题仍使用深色正文和 `--neon-text`，会出现深色文字叠在深色表面上的低对比度问题。

**涉及文件**

- `web/src/styles/neon.css`

**建议**

- 将降级规则限制在 `[data-theme-preset='neon-glass']` 范围内。
- 亮色降级使用接近 `--card`、`--popover` 的不透明浅色表面。
- `.dark` 下才使用深色的降级表面。
- Tooltip、Popover、Dialog 的文本颜色统一使用相应语义 token，而不是固定 `--neon-text`。

**验收标准**

- 关闭或不支持 backdrop blur 时，亮色正文仍达到可读对比度。
- 非 Neon Glass 主题不因该降级规则改变表面、阴影和文字颜色。

### 4.2 服务端默认主题导致首屏闪烁和重复请求

**现象**

无用户主题 cookie 时，应用先按本地 `system` 默认主题渲染，再由 `ThemeProvider` 请求 `/api/status` 并切换成管理员配置的主题。用户系统主题与管理员默认主题不一致时，首屏会明显闪烁。主题切换后 effect 还可能再次请求同一接口。

**涉及文件**

- `web/src/context/theme-provider.tsx`
- `web/src/main.tsx`

**建议**

- 将 `default_theme` 合并到已有的应用初始化状态请求中，在首次 React 渲染前决定无 cookie 用户的主题。
- 用户 cookie 优先级最高；仅在 cookie 不存在时使用服务端默认主题。
- `ThemeProvider` 不应为相同状态重复请求 `/api/status`。
- 无法在首帧前拿到状态时，应使用不产生明显反色的加载策略，而不是先完整绘制错误主题。

**验收标准**

- 系统亮色、服务端默认暗色时，无可感知的完整亮色首帧闪烁。
- 系统暗色、服务端默认亮色时，同样无反色闪烁。
- 已主动切换过主题的用户不会被服务端默认主题覆盖。

### 4.3 API Key 默认卡片视图与批量操作功能不等价

**现象**

API Key 表启用了卡片视图和行选择。卡片视图成为默认视图后，卡片不提供复选框、点击选择或键盘选择入口，用户无法在默认视图使用批量操作，必须切回表格。

**涉及文件**

- `web/src/features/keys/components/api-keys-table.tsx`
- `web/src/features/keys/components/api-key-card.tsx`
- `web/src/components/data-table/layout/data-table-page.tsx`

**建议**

- 卡片左上角提供与表格一致的 checkbox。
- 选中状态需要有清晰的可视边框、背景或 ring，并同步 `aria-selected`。
- 触屏用户可以点击 checkbox，键盘用户可以通过 Tab 和 Space 操作。
- 如果卡片视图暂不支持选择，则不应将它作为带批量操作页面的默认视图。

**验收标准**

- 表格视图和卡片视图都能完成同一组批量操作。
- 选中一个或多个卡片后，批量操作栏状态与表格一致。
- 选中状态不只存在于数据属性中，视觉和辅助技术都能识别。

## 5. 配色优化方案

### 5.1 总体原则

保留 Neon Glass 的粉色、紫色、青色、酸橙绿、黄色和红色，但明确区分三类颜色：

1. **品牌色**：只用于品牌焦点和主操作。
2. **语义色**：只用于成功、错误、警告、信息、数据指标。
3. **装饰色**：只用于低透明度背景光晕、描边高光和小范围动效。

不要让同一种紫色同时承担背景、主按钮、标题渐变、hover、边框高光和状态标识。这样会降低控制台的扫描效率。

### 5.2 建议的颜色职责

| 类别 | 建议颜色 | 使用场景 |
| --- | --- | --- |
| 品牌主操作 | 紫色 | 主 CTA、主导航激活、焦点 ring |
| 品牌辅助高光 | 粉色 | 品牌渐变、玻璃边缘、少量 hover 高光 |
| 信息与请求流 | 青色 | API 请求、延迟、文档链接、信息提示 |
| 成功与在线 | 酸橙绿 | 渠道在线、成功、可用、完成 |
| 警告与计费 | 琥珀黄 | 余额、配额接近上限、注意事项 |
| 失败与危险操作 | 红色 | 错误、失败、删除与高风险操作 |
| 中性结构 | 冷灰与靛灰 | 表面、边框、表格、非重点文本 |

### 5.3 亮色模式优化

**现状问题**

亮色背景、白色玻璃层和粉紫光晕叠加后，页面中心容易出现白雾感。终端、次级按钮、输入框和辅助文字的边界较弱。

**建议方向**

- 背景保持带冷感的浅灰紫，不使用纯白。
- 玻璃表面带极低饱和冷紫，避免连续纯白叠层。
- 边框、输入框和分隔线提高不透明度。
- 正文与业务辅助信息不再使用 `text-muted-foreground/50`。

**建议 token 范围**

```css
[data-theme-preset='neon-glass'] {
  --background: oklch(0.965 0.012 285);
  --card: oklch(0.985 0.008 285 / 0.9);
  --glass-g0-bg: oklch(0.985 0.008 285 / 0.94);
  --glass-g1-bg: oklch(0.98 0.01 285 / 0.86);
  --glass-border: oklch(0.3 0.02 285 / 0.22);
  --border: oklch(0.25 0.02 285 / 0.2);
  --input: oklch(0.25 0.02 285 / 0.16);
}
```

数值是起点，实施后必须在真实页面的最终叠色结果中复核对比度。

### 5.4 暗色模式优化

**现状问题**

暗色背景、primary、标题渐变、右下光晕与 hover 均使用紫色，氛围统一但层级同质。深色主按钮配近黑色文字在不同显示器上的可读性余量较小。

**建议方向**

- 保留近黑靛紫舞台和紫色主操作。
- 标题渐变收敛为紫色到青色，避免同时混入过多粉、黄、青。
- 粉色只作为小面积高光，不承担多个主层级。
- 主按钮与 sidebar primary 使用接近白色前景。

**建议 token 调整**

```css
.dark [data-theme-preset='neon-glass'] {
  --primary-foreground: oklch(0.98 0.01 285);
  --sidebar-primary-foreground: oklch(0.98 0.01 285);
}
```

### 5.5 语义颜色的前景色

亮色模式中的成功、警告背景通常较亮，统一使用白色前景容易降低文字清晰度。

建议为成功和警告使用对应深色前景：

```css
[data-theme-preset='neon-glass'] {
  --success-foreground: oklch(0.2 0.02 130);
  --warning-foreground: oklch(0.22 0.03 90);
}
```

暗色模式中偏亮的 success、warning、info 背景同样优先使用深色文字；只有足够深的语义色背景才使用白色文字。

### 5.6 文字与对比度规则

- 正文、时间、状态、金额、配额等业务信息必须达到 WCAG AA 的最终合成对比度。
- 常规正文最小使用 `14px`；紧凑业务信息最小 `12px`。
- `10px` 仅用于完全非必要的装饰标签。
- `text-muted-foreground/50` 只用于不影响理解的纯装饰内容。
- 说明文字优先使用 `text-muted-foreground` 或最高 `/80`，不应再叠加半透明玻璃导致二次变淡。
- 代码示例、表格数值和数据标签需要单独检查，因为它们经常位于半透明表面之上。

## 6. 首页视觉优化

### 6.1 首屏焦点

首页首屏应让产品名称成为第一视觉焦点。当前装饰字 `JPGAPI` 的视觉权重高于不可见的 H1，且占据了主标题应承担的位置。

建议：

- 修复 H1 可见性后，让 H1 保持首屏最大、最清晰的文本层级。
- 装饰字仅作为低对比度背景或小一层的辅助元素。
- 缩小品牌徽章、装饰字、H1、副标题之间的间距，避免视觉断层。
- 移动端隐藏装饰字是正确方向，应继续保持 H1、说明和 CTA 的紧凑垂直节奏。

### 6.2 标题和渐变

- 亮色模式：标题可使用粉色到紫色渐变。
- 暗色模式：标题可使用紫色到青色渐变。
- 黄色和青色不应同时出现在品牌标题中，优先留给警告、状态和 API 数据。
- 渐变只是强调，不应降低主标题文字的辨识度。

### 6.3 背景光晕

- 亮色光晕降低透明度和覆盖范围，避免像大块背景染色。
- 暗色光晕保持空间感，但控制紫色光晕的面积，让青色或粉色只在局部承担层次提示。
- 所有背景光晕必须 `aria-hidden`，并且不影响内容对比度。

## 7. 玻璃材质与组件边界

### 7.1 限定 Neon Glass 的作用范围

当前 `.glass-g0` 到 `.glass-g3` 是全局类，公共 DataTable 与 Card 使用后，非 Neon Glass 主题也会获得模糊、阴影、高光伪元素和额外合成层。

建议：

- 公共组件只提供 `surface`、`elevated`、`clear` 等语义类或 slot。
- 真正的 blur、glass border、shine、重阴影只在 `[data-theme-preset='neon-glass']` 下激活。
- 其他主题保留既有卡片和表格外观，避免此次改版改变全部主题的密度和性能特征。

### 7.2 玻璃层级

建议约定以下语义：

| 层级 | 用途 | 视觉建议 |
| --- | --- | --- |
| G0 | 高密度表格、列表容器 | 最不透明、最弱 blur、最清晰边界 |
| G1 | 常规卡片、设置面板 | 中等不透明度和轻阴影 |
| G2 | 概览数据带、重点区域 | 更强空间层级，但不抢主内容 |
| G3 | Hero、认证卡片、模态重点对象 | 仅用于少量高关注区域 |

不要让每一个容器都升级到 G2/G3；控制台需要稳定的信息密度和清晰分组。

## 8. 动效与性能

### 8.1 动效规则

- 同一元素避免被多个类使用 `animation` 简写。
- 入场动画、持续 shimmer、hover 动效应分到不同元素或使用显式动画列表。
- `prefers-reduced-motion: reduce` 下应停止所有循环、漂移、脉冲和大幅位移动画。
- 任何动画都不能是内容变得可见的唯一条件。

### 8.2 移动端规则

- 小屏和性能受限设备降低 blur 半径、阴影半径和背景光晕数量。
- 对认证页、表格和高密度页面优先保证滚动和点击稳定，倾斜和视差只用于确实能增强理解的模块。
- 不在移动端使用需要持续重绘的大面积滤镜和多层动画背景。

## 9. 测试与验收清单

### 9.1 手动视觉检查

必须分别检查：

- 桌面端：`1280 x 720`。
- 移动端：`390 x 844`。
- 短视口：`390 x 600`。
- 亮色模式。
- 暗色模式。
- 系统主题模式。
- `prefers-reduced-motion: reduce`。
- 不支持 `backdrop-filter` 的降级场景。

关键页面：

- 首页。
- 登录、注册、找回密码。
- 控制台概览。
- API Key 列表的表格和卡片视图。
- 含 Dialog、Popover、Tooltip、Select、Sheet 的设置页面。

### 9.2 自动化回归测试

建议新增针对用户行为的测试，而不是只测试 class 或组件存在：

1. 首页 H1 在普通和减少动态效果下都可见。
2. 短视口认证页面可滚动到提交按钮。
3. 无 backdrop blur 时，亮色 Tooltip、Popover、Dialog 的文字仍具备有效对比度。
4. 无主题 cookie 时服务端默认主题只应用一次；有 cookie 时不覆盖用户选择。
5. API Key 卡片支持勾选、取消勾选、键盘操作和批量操作。
6. Neon Glass 样式不会改变非 Neon Glass preset 的公共表格和 Card 行为。

### 9.3 构建与质量检查

每次变更完成后执行：

```bash
bun --cwd web run typecheck
bun --cwd web run lint
bun --cwd web run build
```

并运行受影响的 Vitest 测试文件。涉及首页、认证布局、主题切换、卡片选择或响应式布局的修改，应补充对应行为回归测试。

## 10. 推荐实施顺序

1. 修复首页 H1 动画冲突与可见性。
2. 修复认证页面短视口滚动和移动端 TiltCard 降级。
3. 修复 `backdrop-filter` 降级的亮暗主题表面颜色。
4. 将服务端默认主题接入应用初始化，消除首屏闪烁和重复请求。
5. 为 API Key 卡片补齐选择与批量操作。
6. 限定 Glass 样式到 Neon Glass preset，减少跨主题影响。
7. 调整亮色背景、边框、输入框与辅助文本对比度。
8. 调整暗色主按钮前景和紫色使用范围，建立清晰的功能色职责。
9. 补齐主题、响应式、动效、无滤镜降级与卡片交互测试。

## 11. 完成定义

本优化完成后，应同时满足：

- 亮色、暗色、移动端和短视口下没有内容不可见、不可达或不可读的问题。
- 首页产品名称明确成为首屏焦点。
- 用户切换主题、服务端设置默认主题、系统主题变化时，不产生明显闪烁或覆盖用户选择。
- 表格和卡片视图提供等价的核心操作能力。
- 玻璃效果是 Neon Glass preset 的可控视觉能力，而不是全局副作用。
- 色彩既保留品牌识别度，也能清晰表达请求、成功、警告、错误和高风险操作等业务语义。
- 所有新增或修复行为都有对应的验证路径和必要的自动化回归保护。
